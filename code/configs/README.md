# Config Layout

- `base.yaml`: default legacy config copied from `code/config.yaml`.
- `modes/`: full training modes such as P-only and P+PP.
- `presets/`: smoke, legacy paper-named presets, and runtime-tuning presets.

The root-level `config*.yaml` files remain as compatibility entrypoints for existing commands.
